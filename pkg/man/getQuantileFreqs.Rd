\name{getQuantileFreqs}
\alias{getQuantileFreqs}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
  Calculate quantile score frequencies.
}
\description{
  %%  ~~ A concise (1-5 lines) description of what the function does. ~~
  For each gene, calculate the frequency of each quantile score.
}
\usage{
getQuantileFreqs(scores, grp1, grp2)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{scores}{data frame of quantile scores generated by
    getQuantileScores, with probes/probesets as rows and samples as
    columns \code{scores}. 
}
  \item{grp1}{vector of indices or sample names for study group 1.
}
  \item{grp2}{vector of indices or sample names for study group 2.
}
}
\details{Additional details?
  %%  ~~ If necessary, more details than the description above ~~
}
\value{
  %%  ~Describe the value returned
  %%  If it is a LIST, use
  %%  \item{comp1 }{Description of 'comp1'}
  %%  \item{comp2 }{Description of 'comp2'}
  %% ...
  Data frame with probes/probesets as rows and quantile score labels for
  each study group as columns. For example, if n.quantiles=4 was
  selected for getQuantileScores, there will be 10 columns, -2 to 2 for
  group 1 and -2 to 2 for group 2.
}
\references{paper?
  %% ~put references to the literature/web site here ~
}
\author{
  Sadik A. Khuder, Peter S. Bazeley
}
\note{additional notes?
  %%  ~~further notes~~
}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{\code{\link{getQuantileScores}}
  %% ~~objects to See Also as \code{\link{help}}, ~~~
}
\examples{
##START

library(QuantCombine)

#Example 1

exprs = data.frame(matrix(rnorm(50),ncol=10))
exprs

#arbitrary groups in this case
grp1 = 1:5
grp2 = 6:10

sc = getQuantileScores(exprs,grp1=grp1,grp2=grp2)
sc

fr = getQuantileFreqs(sc,grp1=grp1,grp2=grp2)
fr

#Example 2 - use data from NCBI GEO

library(GEOquery)

#GSE6872 represents 21 samples hybridized to Affymetrix HGU133 Plus 2.0
# arrays. 
gse = getGEO("GSE6872",GSEMatrix=T)

#to view the organization of the gse object:
#str(gse)

#the sample groups
grps = gse[[1]]@phenoData@data$title
grps

grp1 = grep("Normo",grps)
grp1
grp2 = grep("Terato",grps)
grp2

#the expression data
exprs = gse[[1]]@assayData$exprs
exprs[1:5,]

sc = getQuantileScores(exprs,grp1,grp2)
sc[1:5,]

fr = getQuantileFreqs(sc,grp1=grp1,grp2=grp2)
fr[1:5,]

##END
}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ ~kwd1 }
\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
